<Window
    x:Class="BestLogViewer.About"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:BestLogViewer"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="clr-namespace:BestLogViewer.Models"
    xmlns:vm="clr-namespace:BestLogViewer.ViewModels"
    Title="About"
    Width="500"
    Height="500"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">
    <Window.DataContext>
        <vm:AboutViewModel x:Name="Vm" />
    </Window.DataContext>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock
            Margin="10"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            FontSize="20"
            TextTrimming="WordEllipsis"
            TextWrapping="Wrap">
            Best Log Viewer. Drag a log, set keyword colors, and convert to HTML for highlighting.<LineBreak />
            The source code is available on GitHub at<Hyperlink NavigateUri="https://github.com/hillsburg/BestLogViewer" ToolTip="https://github.com/hillsburg/BestLogViewer">Here</Hyperlink>
        </TextBlock>
        <StackPanel
            Grid.Row="1"
            Margin="10"
            Orientation="Vertical">
            <StackPanel Orientation="Horizontal">
                <TextBlock
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    FontSize="20"
                    Text="Current version:" />
                <TextBlock
                    Margin="10,0,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    FontSize="20"
                    Foreground="DeepPink"
                    Text="{Binding AppVersion}" />
            </StackPanel>
            <StackPanel Margin="0,5,0,0">
                <Button
                    Command="{Binding OpenUrlCommand}"
                    CommandParameter="{Binding AppVersion}"
                    Content="GO &amp; Check Version !" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
